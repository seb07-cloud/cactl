---
phase: 02-state-and-import
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/normalize/normalize.go
  - internal/normalize/normalize_test.go
  - internal/normalize/slug.go
  - internal/normalize/slug_test.go
autonomous: true
requirements: [IMPORT-03, IMPORT-04, IMPORT-05, IMPORT-06]

must_haves:
  truths:
    - "Server-managed fields (id, createdDateTime, modifiedDateTime, templateId) are stripped from Graph API JSON"
    - "Explicit null values are recursively removed from nested maps and arrays"
    - "@odata.* metadata keys are recursively stripped at every nesting level"
    - "Output JSON has alphabetically sorted keys and 2-space indented pretty-print with trailing newline"
    - "Display names are converted to kebab-case slugs (lowercase, non-alphanumeric replaced with hyphens, no leading/trailing dashes)"
    - "Empty arrays are preserved (semantically meaningful in CA policies)"
  artifacts:
    - path: "internal/normalize/normalize.go"
      provides: "Normalize function: strip server fields, strip @odata, remove nulls, sort keys, pretty-print"
      exports: ["Normalize"]
    - path: "internal/normalize/slug.go"
      provides: "Slugify function: display name to kebab-case slug"
      exports: ["Slugify"]
    - path: "internal/normalize/normalize_test.go"
      provides: "Table-driven tests for normalization pipeline"
      contains: "func Test"
    - path: "internal/normalize/slug_test.go"
      provides: "Table-driven tests for slug derivation"
      contains: "func Test"
  key_links:
    - from: "internal/normalize/normalize.go"
      to: "encoding/json"
      via: "Unmarshal to map, MarshalIndent for sorted keys"
      pattern: "json\\.MarshalIndent"
    - from: "internal/normalize/slug.go"
      to: "regexp"
      via: "Compiled regex for non-alphanumeric replacement"
      pattern: "regexp\\.MustCompile"
---

<objective>
JSON normalization pipeline and kebab-case slug derivation for CA policy import.

Purpose: Import needs deterministic, canonical JSON output so that re-importing the same policy produces identical bytes. Slug derivation provides the keying mechanism for state storage. Both are pure functions with clear input/output contracts -- ideal for TDD.

Output: `internal/normalize/` package with Normalize() and Slugify() functions, fully tested.
</objective>

<execution_context>
@/Users/seb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-and-import/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD normalize package — strip, null-remove, sort, pretty-print</name>
  <files>internal/normalize/normalize.go, internal/normalize/normalize_test.go</files>
  <action>
RED: Create `internal/normalize/normalize_test.go` with table-driven tests:
- **Strip server fields:** Input JSON with `id`, `createdDateTime`, `modifiedDateTime`, `templateId` fields → output has none of them
- **Strip @odata fields:** Input with `@odata.context` at top level AND nested `authenticationStrength@odata.context` inside `grantControls` → both stripped
- **Remove nulls:** Input with `"sessionControls": null` and nested `"platforms": null` inside `conditions` → both removed
- **Preserve empty arrays:** Input with `"excludeUsers": []` → output still has `"excludeUsers": []`
- **Sort keys:** Input with keys in random order → output has alphabetical keys at all nesting levels
- **Pretty-print:** Output uses 2-space indent and ends with trailing newline
- **Full pipeline:** Use the exact Graph API example from research (the "CA001: Require MFA for admins" response) → verify exact expected output bytes

Use `testify/assert` for assertions. Each test case: name, input JSON bytes, expected output JSON bytes (or error).

Run tests → all MUST fail (Normalize function doesn't exist yet).

GREEN: Create `internal/normalize/normalize.go` with:
- Package-level `serverManagedFields` slice: `["id", "createdDateTime", "modifiedDateTime", "templateId"]`
- `Normalize(raw []byte) ([]byte, error)` function:
  1. `json.Unmarshal` into `map[string]interface{}`
  2. Delete server-managed fields from top-level map
  3. Call `stripODataFields(m)` — recursive, deletes any key containing `@odata.` (not just prefixed — Graph API uses `fieldName@odata.context` pattern)
  4. Call `removeNulls(m)` — recursive over nested maps and arrays of maps
  5. `json.MarshalIndent(m, "", "  ")` — Go maps auto-sort keys on marshal
  6. Append trailing `'\n'`
- `removeNulls(m map[string]interface{})` — deletes nil values, recurses into nested maps and `[]interface{}` arrays
- `stripODataFields(m map[string]interface{})` — deletes keys containing `@odata.`, recurses into nested maps and arrays

Run tests → all MUST pass.

REFACTOR: Only if needed. Ensure exported function has godoc comment.

Commit RED: `test(02-01): add failing tests for JSON normalization pipeline`
Commit GREEN: `feat(02-01): implement JSON normalization (strip, null-remove, sort, pretty-print)`
  </action>
  <verify>
`cd /Users/seb/Git/cactl && go test ./internal/normalize/ -run TestNormalize -v` — all tests pass.
`cd /Users/seb/Git/cactl && go vet ./internal/normalize/` — no issues.
  </verify>
  <done>Normalize function strips server fields, strips @odata recursively, removes nulls recursively, preserves empty arrays, sorts keys alphabetically, and produces 2-space indented JSON with trailing newline. Full pipeline test matches expected output byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 2: TDD slug derivation — display name to kebab-case</name>
  <files>internal/normalize/slug.go, internal/normalize/slug_test.go</files>
  <action>
RED: Create `internal/normalize/slug_test.go` with table-driven tests:
- `"CA001: Require MFA for admins"` → `"ca001-require-mfa-for-admins"`
- `"Block Legacy Authentication"` → `"block-legacy-authentication"`
- `"CA 001"` → `"ca-001"`
- `"  Leading Spaces  "` → `"leading-spaces"` (no leading/trailing dashes)
- `"ALL---CAPS---POLICY"` → `"all-caps-policy"` (consecutive special chars collapse to single dash)
- `"policy_with_underscores"` → `"policy-with-underscores"`
- `""` → `""` (empty string edge case)
- `"simple"` → `"simple"` (already kebab-case)

Run tests → all MUST fail.

GREEN: Create `internal/normalize/slug.go` with:
- Package-level compiled regexes: `nonAlphanumRegex = regexp.MustCompile("[^a-z0-9]+")` and `leadTrailDash = regexp.MustCompile("^-+|-+$")`
- `Slugify(displayName string) string`:
  1. `strings.ToLower(displayName)`
  2. `nonAlphanumRegex.ReplaceAllString(s, "-")` — collapses any run of non-alphanumeric chars to single dash
  3. `leadTrailDash.ReplaceAllString(s, "")` — trim leading/trailing dashes

Run tests → all MUST pass.

Commit RED: `test(02-01): add failing tests for kebab-case slug derivation`
Commit GREEN: `feat(02-01): implement Slugify for kebab-case slug derivation`
  </action>
  <verify>
`cd /Users/seb/Git/cactl && go test ./internal/normalize/ -run TestSlugify -v` — all tests pass.
`cd /Users/seb/Git/cactl && go test ./internal/normalize/ -v` — ALL normalize package tests pass together.
  </verify>
  <done>Slugify converts display names to kebab-case slugs. Handles colons, spaces, underscores, consecutive special characters, leading/trailing whitespace, and empty strings. All 8+ test cases pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/seb/Git/cactl && go test ./internal/normalize/ -v -count=1
cd /Users/seb/Git/cactl && go vet ./internal/normalize/
```
All normalize tests pass. No vet issues.
</verification>

<success_criteria>
- Normalize() produces deterministic canonical JSON from Graph API responses
- Slugify() produces valid kebab-case slugs from any display name
- Both functions have comprehensive table-driven tests
- Full pipeline test uses realistic Graph API response and verifies exact output
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-and-import/02-01-SUMMARY.md`
</output>
