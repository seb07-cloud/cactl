---
phase: 05-production-readiness
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - README.md
  - docs/getting-started.md
  - docs/multi-tenant.md
  - docs/ci-cd.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "README has badges, install instructions, quick start, and architecture overview"
    - "Getting started guide walks through install, init, import, plan, apply"
    - "Multi-tenant guide explains --tenant with multiple values and per-tenant credentials"
    - "CI/CD guide covers GitHub Actions OIDC and Azure DevOps SP cert with references to example pipelines"
  artifacts:
    - path: "README.md"
      provides: "Project README with badges, install, quick start, architecture"
      contains: "cactl"
    - path: "docs/getting-started.md"
      provides: "Getting started guide"
      contains: "cactl init"
    - path: "docs/multi-tenant.md"
      provides: "Multi-tenant usage guide"
      contains: "--tenant"
    - path: "docs/ci-cd.md"
      provides: "CI/CD integration guide"
      contains: "workload identity"
  key_links:
    - from: "README.md"
      to: "docs/getting-started.md"
      via: "link to getting started guide"
      pattern: "getting-started"
    - from: "docs/ci-cd.md"
      to: "examples/github-actions/cactl-plan.yml"
      via: "references example workflow"
      pattern: "cactl-plan\\.yml"
---

<objective>
Create the documentation suite: README with badges, getting-started guide, multi-tenant guide, and CI/CD integration guide.

Purpose: Provide users with comprehensive documentation covering installation, first use, multi-tenant workflows, and CI/CD integration patterns.

Output: `README.md`, `docs/getting-started.md`, `docs/multi-tenant.md`, `docs/ci-cd.md`.
</objective>

<execution_context>
@/Users/seb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-readiness/05-RESEARCH.md
@.planning/phases/05-production-readiness/05-01-SUMMARY.md
@.planning/phases/05-production-readiness/05-02-SUMMARY.md
@.planning/phases/05-production-readiness/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README with badges, install, quick start, and architecture</name>
  <files>
    README.md
  </files>
  <action>
Create `README.md` with:

1. **Header**: `# cactl` with one-line description: "CLI deploy framework for Microsoft Entra Conditional Access policies"

2. **Badges** (4 badges on one line):
   - CI status: `![CI](https://github.com/seb07-cloud/cactl/actions/workflows/ci.yml/badge.svg)`
   - Release: `![Release](https://img.shields.io/github/v/release/seb07-cloud/cactl)`
   - License: `![License](https://img.shields.io/badge/license-MIT-blue.svg)`
   - Go version: `![Go](https://img.shields.io/github/go-mod/go-version/seb07-cloud/cactl)`

3. **What is cactl?** section: 2-3 sentences from PROJECT.md core value. Mention the "Friday afternoon problem" -- misconfigured policy locking out users with no version history.

4. **Key Features** section (bullet list): plan/apply safety, Git-native versioning, semantic versioning per policy, multi-tenant support, drift detection, CI/CD ready, single Go binary.

5. **Quick Install** section:
   - Binary download: `curl -sL https://github.com/seb07-cloud/cactl/releases/latest/download/cactl_$(uname -s | tr '[:upper:]' '[:lower:]')_amd64.tar.gz | tar xz && sudo mv cactl /usr/local/bin/`
   - Go install: `go install github.com/seb07-cloud/cactl@latest`
   - Verify: `cactl --version`

6. **Quick Start** section (numbered steps):
   1. `cactl init` -- scaffold workspace
   2. `cactl import --all --tenant YOUR_TENANT_ID` -- import live policies
   3. Edit policy JSON files
   4. `cactl plan --tenant YOUR_TENANT_ID` -- preview changes
   5. `cactl apply --tenant YOUR_TENANT_ID` -- deploy changes

7. **Architecture** section: Brief overview of the pipeline (CLI -> Auth -> Graph API -> Normalize -> State -> Git). Mention the key packages: `cmd/`, `internal/auth/`, `internal/graph/`, `internal/normalize/`, `internal/state/`, `pkg/types/`.

8. **Documentation** section: Links to `docs/getting-started.md`, `docs/multi-tenant.md`, `docs/ci-cd.md`.

9. **Required Permissions** section: List Graph API permissions (Policy.Read.All, Policy.ReadWrite.ConditionalAccess, Application.Read.All, Group.Read.All, RoleManagement.Read.Directory).

10. **License** section: "MIT -- see [LICENSE](LICENSE)"
  </action>
  <verify>
    `test -f /Users/seb/Git/cactl/README.md && wc -l /Users/seb/Git/cactl/README.md` -- file exists and has substantial content (>50 lines).
  </verify>
  <done>
    README.md has badges, install instructions, quick start, architecture overview, documentation links, and license reference.
  </done>
</task>

<task type="auto">
  <name>Task 2: Getting started, multi-tenant, and CI/CD guides</name>
  <files>
    docs/getting-started.md
    docs/multi-tenant.md
    docs/ci-cd.md
  </files>
  <action>
1. Create `docs/getting-started.md` (DOCS-01):
   - **Install**: Binary download (linux/mac/windows) and `go install`
   - **Initialize workspace**: `cactl init`, explain what it creates (.cactl/config.yaml, .gitignore entry, schema fetch)
   - **Configure authentication**: Show three auth modes with env var examples:
     - Azure CLI: `az login` then `cactl import --all --tenant TENANT_ID`
     - SP secret: `export CACTL_CLIENT_ID=... CACTL_CLIENT_SECRET=...`
     - SP certificate: `export CACTL_CLIENT_ID=... CACTL_CERT_PATH=...`
   - **First import**: `cactl import --all --tenant YOUR_TENANT_ID` with expected output
   - **First plan**: Edit a policy file, run `cactl plan --tenant YOUR_TENANT_ID`
   - **First apply**: `cactl apply --tenant YOUR_TENANT_ID` with confirmation prompt
   - **What's next**: Links to multi-tenant and CI/CD guides

2. Create `docs/multi-tenant.md` (DOCS-02):
   - **Overview**: Explain sequential multi-tenant execution model
   - **Passing multiple tenants**: Show `--tenant ID1 --tenant ID2` and `--tenant ID1,ID2` syntax
   - **Per-tenant credentials**: Explain ClientFactory per-tenant isolation, env var setup for each tenant
   - **Exit codes**: Explain aggregation (highest severity wins: 2 > 1 > 0)
   - **Separate repos per tenant**: Explain the chosen model (cleanest isolation)
   - **Limitations**: Concurrent pipeline applies not safe in v1 (MTNT-04 advisory)

3. Create `docs/ci-cd.md` (DOCS-03):
   - **Overview**: Non-interactive mode with `--ci` flag
   - **CI mode behavior**: `--ci` suppresses prompts, requires `--auto-approve` for write ops, requires `--all` or `--policy` for import
   - **GitHub Actions with OIDC**: Step-by-step setup (app registration, federated credential, GitHub environment variables), reference `examples/github-actions/cactl-plan.yml`
   - **Azure DevOps with SP certificate**: Step-by-step setup (SP creation, cert upload, variable group), reference `examples/azure-devops/azure-pipelines.yml`
   - **Scheduled drift detection**: Explain daily cron pattern, exit code alerting, reference `examples/github-actions/cactl-drift.yml`
   - **Exit code contract**: Table of exit codes (0=success, 1=changes/drift, 2=fatal, 3=validation)
  </action>
  <verify>
    `test -f /Users/seb/Git/cactl/docs/getting-started.md && test -f /Users/seb/Git/cactl/docs/multi-tenant.md && test -f /Users/seb/Git/cactl/docs/ci-cd.md && echo "All docs exist"`.
  </verify>
  <done>
    Getting-started guide covers install through first apply. Multi-tenant guide explains sequential execution and per-tenant credentials. CI/CD guide covers GitHub Actions OIDC, Azure DevOps SP cert, and scheduled drift checks.
  </done>
</task>

</tasks>

<verification>
- `README.md` has badges, install, quick start, architecture, doc links
- `docs/getting-started.md` walks through install -> init -> import -> plan -> apply
- `docs/multi-tenant.md` explains --tenant with multiple values, exit code aggregation
- `docs/ci-cd.md` covers GitHub Actions OIDC, Azure DevOps SP cert, scheduled drift
- All internal links are valid (relative paths between docs)
</verification>

<success_criteria>
- All four documentation files exist with comprehensive content
- README serves as project landing page with all essential information
- Guides are actionable -- user can follow steps without external research
- CI/CD guide references the example pipelines created in 05-02
- Multi-tenant guide documents MTNT-04 advisory limitation
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-readiness/05-04-SUMMARY.md`
</output>
