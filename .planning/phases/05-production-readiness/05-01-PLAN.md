---
phase: 05-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/root.go
  - cmd/helpers.go
  - cmd/import.go
  - pkg/types/config.go
  - internal/config/config.go
  - internal/config/validate.go
autonomous: true
requirements:
  - MTNT-01
  - MTNT-02
  - MTNT-03
  - MTNT-04
  - CICD-01
  - CICD-02

must_haves:
  truths:
    - "User can pass --tenant with multiple values and each command executes sequentially per tenant"
    - "Exit code reflects highest severity across all tenant executions (2 > 1 > 0)"
    - "Running with --ci suppresses all interactive prompts and returns validation error if input would be needed"
    - "Running with --ci on a write operation without --auto-approve returns exit code 3"
    - "MTNT-04 advisory documented: concurrent pipeline applies are not safe in v1"
  artifacts:
    - path: "cmd/root.go"
      provides: "--tenant as StringSlice flag, --auto-approve global flag"
      contains: "StringSlice"
    - path: "cmd/helpers.go"
      provides: "runForTenants sequential execution loop, requireApproveInCI guard"
      exports: ["runForTenants", "requireApproveInCI"]
    - path: "pkg/types/config.go"
      provides: "Tenants field as []string on Config"
      contains: "Tenants"
  key_links:
    - from: "cmd/helpers.go"
      to: "internal/auth/factory.go"
      via: "factory.Credential(ctx, tenantID) per loop iteration"
      pattern: "factory\\.Credential"
    - from: "cmd/import.go"
      to: "cmd/helpers.go"
      via: "runForTenants called in RunE"
      pattern: "runForTenants"
---

<objective>
Add multi-tenant sequential execution and CI mode safety guards to the CLI.

Purpose: Enable `--tenant` to accept multiple tenant IDs with sequential execution per tenant, and harden CI mode by enforcing `--auto-approve` on write operations.

Output: Updated root flags, `runForTenants` helper, CI guard function, updated import command using multi-tenant loop.
</objective>

<execution_context>
@/Users/seb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-readiness/05-RESEARCH.md
@cmd/root.go
@cmd/import.go
@pkg/types/config.go
@internal/config/config.go
@internal/config/validate.go
@internal/auth/factory.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-tenant flag, config, and sequential execution helper</name>
  <files>
    cmd/root.go
    cmd/helpers.go
    pkg/types/config.go
    internal/config/config.go
    internal/config/validate.go
  </files>
  <action>
1. In `cmd/root.go`, change the `--tenant` flag from `String` to `StringSlice`:
   - Replace `rootCmd.PersistentFlags().String("tenant", "", ...)` with `rootCmd.PersistentFlags().StringSlice("tenant", nil, "Entra tenant ID(s) -- supports multiple values")`
   - Add a new global flag: `rootCmd.PersistentFlags().Bool("auto-approve", false, "Skip confirmation prompts (required with --ci for write operations)")`

2. In `pkg/types/config.go`, change `Tenant string` to `Tenants []string` with mapstructure tag `"tenant"` (viper StringSlice binds to this). Keep backward compatibility by adding a helper method `Config.FirstTenant() string` that returns `Tenants[0]` or `""`.

3. In `internal/config/config.go`, update `Load()`:
   - Replace `v.GetString("tenant")` usage with `v.GetStringSlice("tenant")` and assign to `cfg.Tenants`
   - If `cfg.Tenants` is empty but `v.GetString("tenant")` returns a non-empty string, wrap it in a single-element slice (backward compat with env var `CACTL_TENANT=abc`)

4. In `internal/config/validate.go`, update validation to check `len(cfg.Tenants) > 0` where tenant is required (import, plan, apply, drift commands -- but don't add command-specific validation here; just update any existing tenant validation).

5. Create `cmd/helpers.go` with two functions:
   - `runForTenants(ctx context.Context, tenants []string, authCfg types.AuthConfig, fn func(ctx context.Context, tenantID string, cred azcore.TokenCredential) error) error` -- Creates a `ClientFactory`, loops over tenants, calls `factory.Credential(ctx, tenantID)` for each, calls `fn`, tracks max exit code across iterations. If any tenant returns ExitChanges (1), overall exit is 1. If any returns ExitFatalError (2), overall exit is 2. Logs per-tenant header when `len(tenants) > 1`.
   - `requireApproveInCI(ciMode, autoApprove bool) error` -- Returns `ExitValidationError` if `ciMode && !autoApprove` with message "--ci mode requires --auto-approve for write operations".

6. For MTNT-04 (concurrent pipeline advisory): No code enforcement in v1. Add a comment in `cmd/helpers.go`: `// MTNT-04: Concurrent pipeline applies are not safe in v1. Advisory only -- defer lock file to v1.1.`
  </action>
  <verify>
    `cd /Users/seb/Git/cactl && go build ./...` compiles without errors. `go vet ./...` passes.
  </verify>
  <done>
    --tenant accepts StringSlice values, Config.Tenants is []string, runForTenants loops with exit code aggregation, requireApproveInCI returns validation error when CI mode lacks --auto-approve.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire import command to multi-tenant execution</name>
  <files>
    cmd/import.go
  </files>
  <action>
1. Refactor `runImport` to use the multi-tenant pattern:
   - Extract the core import logic (from "Load config" onward) into an inner function that receives `tenantID string` and `cred azcore.TokenCredential`
   - Load config once at the top of RunE, read `cfg.Tenants`
   - If `len(cfg.Tenants) == 0`, return ExitValidationError: "tenant is required (set via --tenant flag, CACTL_TENANT env, or config file)"
   - Call `runForTenants(ctx, cfg.Tenants, cfg.Auth, func(ctx, tenantID, cred) error { ... })` where the inner function:
     - Creates `graph.NewClient(cred, tenantID)`
     - Creates `state.NewGitBackend(".")`
     - Runs the existing import pipeline (fetch, manifest, filter, normalize, write, tag)
   - Replace `cfg.Tenant` references with the `tenantID` parameter

2. Keep all existing flags (--all, --policy, --force) working unchanged.

3. Ensure `ciMode` check still works: read from `v.GetBool("ci")` as before.
  </action>
  <verify>
    `cd /Users/seb/Git/cactl && go build ./... && go test ./cmd/... -run TestImport -count=1` -- build succeeds and existing tests pass (or skip gracefully if they require Graph API).
  </verify>
  <done>
    Import command uses runForTenants for sequential multi-tenant execution. Single-tenant usage unchanged. CI mode guard functional.
  </done>
</task>

</tasks>

<verification>
- `go build ./...` succeeds
- `go vet ./...` passes
- `go test ./... -count=1` passes (all existing tests)
- `--tenant` flag accepts both `--tenant a --tenant b` and `--tenant a,b` syntax (verified by `go run . --help` showing StringSlice type)
</verification>

<success_criteria>
- Multi-tenant --tenant flag accepts []string values
- Sequential execution loop processes each tenant with isolated credentials
- Exit code aggregation returns highest severity across tenants
- CI mode guard rejects write operations without --auto-approve
- Import command refactored to use multi-tenant helper
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-readiness/05-01-SUMMARY.md`
</output>
